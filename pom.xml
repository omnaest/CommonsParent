<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>org.omnaest.utils</groupId>
	<artifactId>CommonsParent</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<packaging>pom</packaging>
	<name>CommonsParent</name>
	<description>Parent POM providing shared build configuration for omnaest Commons utilities.</description>
	<url>https://github.com/omnaest/CommonsParent</url>
	<scm>
		<connection>scm:git:git://github.com/omnaest/CommonsParent.git</connection>
		<developerConnection>scm:git:ssh://github.com/omnaest/CommonsParent.git</developerConnection>
		<url>https://github.com/omnaest/CommonsParent</url>
	</scm>
	<licenses>
		<license>
			<name>The Apache Software License, Version 2.0</name>
			<url>http://www.apache.org/licenses/LICENSE-2.0.txt</url>
			<distribution>repo</distribution>
		</license>
	</licenses>
	<developers>
		<developer>
			<id>omnaest</id>
			<name>Danny Kunz</name>
			<email>awonderland6@googlemail.com</email>
		</developer>
	</developers>
	<properties>
		<!-- Without these, javac and every report plugin fall back to the platform default encoding, which is Cp1252 on
		     the Windows build machines and UTF-8 on Linux CI - so the same source produced different constants depending
		     on where it was compiled. UTF-8 is declared rather than windows-1252 because the workspace source is already
		     overwhelmingly UTF-8 (351 files carry multi-byte characters); the 21 files that were still Cp1252 were
		     transcoded to UTF-8 alongside this change, so a single declaration is now correct for every child project.
		     Note this is deliberately NOT the ISO-8859-1 that spotless-maven-plugin declares below: that setting exists
		     to make spotless byte-preserving over files of unknown encoding, and is unrelated to how javac reads them. -->
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
		<!-- One place, because the delombok plugin below must run the SAME lombok this compiles against. -->
		<lombok.version>1.18.46</lombok.version>
		<java.version>17</java.version>
		<maven.compiler.release>${java.version}</maven.compiler.release>
		<maven.compiler.source>${java.version}</maven.compiler.source>
		<maven.compiler.target>${java.version}</maven.compiler.target>
		<main.class/>
	</properties>
	<distributionManagement>
		<snapshotRepository>
			<id>ossrh</id>
			<url>https://central.sonatype.com/repository/maven-snapshots/</url>
		</snapshotRepository>
		<!-- No release <repository> entry: central-publishing-maven-plugin (extensions=true, below) intercepts the
		     deploy goal for releases and uploads via the Central Portal Publisher API using publishingServerId for
		     credentials; it does not consult distributionManagement's release repository URL at all, so a
		     release-staging entry here would be vestigial. -->
	</distributionManagement>
	<repositories>
		<repository>
			<id>ossrh</id>
			<url>https://central.sonatype.com/repository/maven-snapshots/</url>
			<snapshots>
				<enabled>true</enabled>
			</snapshots>
		</repository>
	</repositories>
	<dependencyManagement>
		<dependencies>
			<!-- Jackson is declared by a number of Commons and downstream projects, each of which used to pin its
			     own version - 2.8.7, 2.9.3, 2.9.4 and 2.16.2 were all in use at the same time. Importing the BOM
			     here aligns every child on one version, keeps the artifacts of the Jackson family consistent with
			     each other, and turns the next security advisory into a single edit in this file.

			     2.18.9 is the lowest version that clears all seven Dependabot advisories open against
			     CommonsJSONAndXML: GHSA-5jmj-h7xm-6q6v is only fixed in 2.18.9, the remaining six in 2.18.8 or
			     earlier. Child projects that need a different version can still override this by declaring an
			     explicit <version>, since a dependency declaration wins over imported management. -->
			<dependency>
				<groupId>com.fasterxml.jackson</groupId>
				<artifactId>jackson-bom</artifactId>
				<version>2.22.2</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>
	<dependencies>
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<version>${lombok.version}</version>
			<scope>provided</scope>
		</dependency>
	</dependencies>
	<build>
		<plugins>
			<plugin>
				<groupId>org.sonatype.central</groupId>
				<artifactId>central-publishing-maven-plugin</artifactId>
				<version>0.11.0</version>
				<extensions>true</extensions>
				<configuration>
					<publishingServerId>ossrh</publishingServerId>
				</configuration>
			</plugin>
			<plugin>
				<groupId>com.diffplug.spotless</groupId>
				<artifactId>spotless-maven-plugin</artifactId>
				<version>2.43.0</version>
				<dependencies>
					<dependency>
						<groupId>org.omnaest.utils</groupId>
						<artifactId>CommonsSpotless</artifactId>
						<version>0.0.1-SNAPSHOT</version>
					</dependency>
				</dependencies>
				<configuration>
					<!-- ISO-8859-1 handles both UTF-8 (ASCII subset) and Windows-1252 files without error -->
					<encoding>ISO-8859-1</encoding>
					<java>
						<eclipse>
							<file>spotless/eclipse-formatter.xml</file>
							<version>4.22</version>
						</eclipse>
						<importOrder>
							<order>java,javax,org,com,</order>
						</importOrder>
					</java>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.13.0</version>
				<configuration>
					<release>${maven.compiler.release}</release>
					<source>${maven.compiler.source}</source>
					<target>${maven.compiler.target}</target>
				</configuration>
			</plugin>
			<plugin>
				<artifactId>maven-assembly-plugin</artifactId>
				<version>3.3.0</version>
				<configuration>
					<archive>
						<manifest>
							<mainClass>${main.class}</mainClass>
						</manifest>
					</archive>
					<descriptorRefs>
						<descriptorRef>jar-with-dependencies</descriptorRef>
					</descriptorRefs>
				</configuration>
				<executions>
					<execution>
						<id>make-assembly</id>
						<phase>package</phase>
						<goals>
							<goal>single</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-source-plugin</artifactId>
				<version>3.4.0</version>
				<executions>
					<execution>
						<id>attach-sources</id>
						<goals>
							<goal>jar-no-fork</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<!--
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-release-plugin</artifactId>
                <version>2.5.3</version>
                <configuration>
                    <autoVersionSubmodules>true</autoVersionSubmodules>
                    <useReleaseProfile>false</useReleaseProfile>
                    <releaseProfiles>release</releaseProfiles>
                    <goals>deploy</goals>
                </configuration>
            </plugin>-->
			<!--
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-deploy-plugin</artifactId>
                <version>2.8.2</version>
            </plugin>-->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-jar-plugin</artifactId>
				<version>3.2.0</version>
				<configuration>
					<archive>
						<manifest>
							<mainClass>${main.class}</mainClass>
						</manifest>
					</archive>
				</configuration>
			</plugin>
		</plugins>
	</build>
	<profiles>
		<profile>
			<id>release</id>
			<build>
				<plugins>
					<!--
					    Javadoc runs on RELEASE only, not on every build.

					    Maven Central requires a -javadoc.jar, so it has to be produced when publishing. It does
					    not have to be produced when someone runs `mvn install` locally, and producing it there
					    cost every build in this workspace a few seconds and roughly two hundred lines of
					    "cannot find symbol: class *DataBuilder" errors.

					    Those errors are real, not cosmetic: javadoc does not run annotation processing, so it
					    cannot see anything Lombok generates - and hand-written code across this workspace
					    imports those generated types by name (e.g. AnkerData.AnkerDataBuilder). With
					    failOnError=false the build stayed green and the output was discarded, which is why it
					    went unnoticed that the attached javadoc jar is EMPTY: 317 bytes, zero HTML files.

					    Moving the executions here changes none of that - it only stops ordinary builds paying
					    for, and printing, a failure that belongs to publishing. The empty-jar defect is
					    untouched and still needs delombok (or an equivalent) to fix properly.
					-->
					<!--
					    Delombok first, so javadoc has something it can actually read.

					    Javadoc does not run annotation processing, so it cannot resolve anything Lombok
					    generates - and code across this workspace imports those types by name, e.g.
					    AnkerData.AnkerDataBuilder. The result was an attached -javadoc.jar of 317 bytes
					    containing zero HTML pages, on every artifact published from here. failOnError=false kept
					    the builds green and discarded the output, which is why it went unnoticed.

					    Delombok writes plain Java with the annotations expanded; javadoc reads that instead.
					    Proven on react4j-core-components before being put here: 317 bytes -> 3.2 MB, 0 -> 1190
					    HTML pages, ~200 javadoc errors -> 0, with AnkerData.AnkerDataBuilder itself documented.

					    addOutputDirectory=false is load-bearing. Without it the delomboked sources become a
					    second compile root and every class is compiled twice.

					    The plugin's last release is from 2021 and bundles a lombok of that vintage, so its
					    dependency is overridden with the version this workspace compiles against.
					-->
					<plugin>
						<groupId>org.projectlombok</groupId>
						<artifactId>lombok-maven-plugin</artifactId>
						<version>1.18.20.0</version>
						<dependencies>
							<dependency>
								<groupId>org.projectlombok</groupId>
								<artifactId>lombok</artifactId>
								<version>${lombok.version}</version>
							</dependency>
						</dependencies>
						<executions>
							<execution>
								<id>delombok</id>
								<phase>generate-sources</phase>
								<goals>
									<goal>delombok</goal>
								</goals>
								<configuration>
									<sourceDirectory>src/main/java</sourceDirectory>
									<outputDirectory>${project.build.directory}/generated-sources/delombok</outputDirectory>
									<addOutputDirectory>false</addOutputDirectory>
								</configuration>
							</execution>
						</executions>
					</plugin>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-javadoc-plugin</artifactId>
						<version>3.12.0</version>
						<configuration>
							<failOnError>false</failOnError>
							<doclint>none</doclint>
							<sourcepath>${project.build.directory}/generated-sources/delombok</sourcepath>
						</configuration>
						<executions>
							<execution>
								<id>generate-javadoc</id>
								<phase>prepare-package</phase>
								<goals>
									<goal>javadoc</goal>
								</goals>
							</execution>
							<execution>
								<id>attach-javadocs</id>
								<goals>
									<goal>jar</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-gpg-plugin</artifactId>
						<version>3.2.2</version>
						<executions>
							<execution>
								<id>sign-artifacts</id>
								<phase>verify</phase>
								<goals>
									<goal>sign</goal>
								</goals>
							</execution>
						</executions>
						<configuration>
							<skip>true</skip>
						</configuration>
					</plugin>
					<plugin>
						<groupId>org.codehaus.mojo</groupId>
						<artifactId>license-maven-plugin</artifactId>
						<version>2.4.0</version>
						<executions>
							<execution>
								<id>add-third-party</id>
								<phase>generate-resources</phase>
								<goals>
									<goal>add-third-party</goal>
								</goals>
							</execution>
							<execution>
								<id>download-licenses</id>
								<goals>
									<goal>download-licenses</goal>
								</goals>
							</execution>
						</executions>
						<configuration>
							<excludedScopes>test,provided,system</excludedScopes>
						</configuration>
					</plugin>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-dependency-plugin</artifactId>
						<version>3.6.1</version>
						<executions>
							<execution>
								<id>analyze</id>
								<goals>
									<goal>analyze-only</goal>
								</goals>
								<configuration>
									<failOnWarning>false</failOnWarning>
								</configuration>
							</execution>
						</executions>
					</plugin>
					<!-- nexus-staging-maven-plugin (legacy OSSRH Nexus staging/close/release) removed: it targeted
					     the decommissioned oss.sonatype.org host and is fully superseded by
					     central-publishing-maven-plugin (declared above, extensions=true), which handles
					     upload/validate/publish against the Central Portal for both snapshots and releases.
					     Keeping both configured was contradictory. -->
				</plugins>
			</build>
		</profile>
	</profiles>
</project>